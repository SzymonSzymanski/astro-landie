---
const { src, alt, className } = Astro.props;
---

<img class={className ? className : ''} {src} {alt} />

<style lang="scss">
  img {
    display: block;
    width: 3rem;
    height: 3rem;

    &.small {
      width: auto;
      height: auto;
    }
  }
</style>
